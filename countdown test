import time


my_deadline_time = int(input("Quick, enter your deadline time in seconds!"))

for x in range(my_deadline_time,0,-1):
    seconds = x % 60
    minutes = int(x/60) % 60
    hours = int(x / 3600)
    print(F"{hours:02}:{minutes:02}:{seconds:02}")
    time.sleep(1)

print("Times up! ")
